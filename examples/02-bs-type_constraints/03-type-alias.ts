///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


/**
 * 类型别名     // 提取公共类型用于后续多处约束
 *
 *
 * type [name_alias] = [common_type]
 */

type userInfo = {
    name: string,
    age: number,
    sex: gender,
};

type gender = 'male' | 'female';

let user: userInfo;

user = {
    name: 'alpha',
    age: 18,
    sex: 'male'
};

function getUserInfo(): userInfo[] {

    return [];
}


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
